# Download from PixVerse

Downloads images from PixVerse account using the CLI. The user must have PixVerse open in their browser and be logged in. The CLI uses the browser session cookies for authentication.